import DBmanage.InitialDatabase;

public class TEst {
	public static void main(String[] args) {
        InitialDatabase.initial();
        InitialDatabase.getConnection();
        InitialDatabase.getConnection();
        InitialDatabase.getConnection();
        InitialDatabase.getConnection();
        InitialDatabase.getConnection();
        InitialDatabase.getConnection();
        InitialDatabase.getConnection();
	}
}
